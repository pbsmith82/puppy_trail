
<div class="card-header" align="center"> User Profile </div>
<br>
<div class="d-flex justify-content-center">
    <%= render 'shared/error' %>
</div>    
<div class="column">
  <b>First Name: </b><%= @user.first_name %><br>
  <b>Last Name: </b><%= @user.last_name %> <br>
  <b>Email: </b><%= @user.email %> <br>
  <b>Age: </b><%= @user.age %> <br>
  <b>Years Experience: </b><%= @user.years_experience %><br>
  <br>
  <a class="btn btn-primary" href="<%= edit_user_path(@user) %>" role="button">Edit User</a>
  <br>
  <br>
</div>
<div class="container">
  <div class="row">
  <div class="col-8" style="height:47px"> <h2>Dogs Walked</h2></div>
  <div class="col-4" style="height:47px" align="right"> <a class="btn btn-primary btn-sm" href="<%= dogs_needs_walked_path %>" role="button">Dogs Needing Walked</a></div></div><br>
  

  
  <div class="container">
   &nbsp; 
  </div>
  <div class="row row-cols-5"> 
      <% @user.dogs.select do |dog| %>
        <div class="card">
          <div class="card-header"> <%= dog.name %> </div>
          <b> Breed:</b> <%= dog.breed %>
          <b> Daily Walks Needed:</b> <%= dog.walks_needed %>
          <br>
          <div class="card-footer">
          <div class="d-grid gap-2 d-md-block">
            <a class="btn btn-secondary btn-sm" href="<%= dog_walks_path(dog.id) %>" role="button">Walk Hx</a>
            <a class="btn btn-success btn-sm" href="<%= new_dog_walk_path(dog.id) %>" role="button">Walk Dog</a>
          </div>
          </div>
        </div>
      <% end%>
  </div>
  <br>
  <br>
  <div class="row">
  <div class="col-8" style="height:47px"> <h2>Dogs Owned</h2></div>
  <div class="col-4" style="height:47px" align="right"> <a class="btn btn-primary btn-sm" href="<%= new_dog_path %>" role="button">Add Dog</a></div></div><br>
  
  <div class="container">
   &nbsp; 
  </div>
  <div class="row row-cols-5"> 
    
      <% @user.owned_dogs.each do |dog| %>
        <div class="card">
          <div class="card-header"> <%= dog.name %> </div>
          <b> Breed:</b> <%= dog.breed %>
          <b> Daily Walks Needed:</b> <%= dog.walks_needed %>
          <br>
          <div class="card-footer">
          <div class="d-grid gap-2 d-md-block">
            <a class="btn btn-secondary btn-sm" href="<%= dog_walks_path(dog.id) %>" role="button">Walk Hx</a>
            <a class="btn btn-primary btn-sm" href="<%= edit_dog_path(dog.id) %>" role="button">Edit Dog</a>
          </div>
          </div>
        </div>
      <% end%>
    
  </div>
</div>

  
